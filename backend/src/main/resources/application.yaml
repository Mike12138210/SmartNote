server:
  port: 8080
file:
  upload-dir: uploads

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/smart_note?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml #mapper映射文件路径
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.Mike12138210.mapper: DEBUG

jwt:
  secret: "bWlrZTEyMTM4bWlrZTEyMTM4bWlrZTEyMTM4bWlrZTEyMTM4"
  expiration: 86400000

ai:
  api:
    key: sk-427c121e32d841a8b05b1b3191645ab3
    url: https://api.deepseek.com/v1/chat/completions
    model: deepseek-chat
  rate-limit:
    max-calls-per-day: 20