#### 4.6

```
- 基础框架：SpringBoot 3.1.5 + MyBatis-Plus 3.5.10 + JWT + BCrypt
- Git 仓库初始化并推送（包含 backend、journal 和空 frontend 占位）

- 注册功能调试通过（密码加密、唯一性检查）
- 登录功能调试通过（JWT 生成与验证）
- 新增笔记功能调试通过（解决 permission 字段枚举类型不匹配问题）
- 列表分页查询当前用户笔记功能调试通过
```

```
遇到的问题与解决
- JWT 解析 userId 类型为 Integer 导致类型转换异常 → 使用 ((Number) userId).longValue() 解决
- 数据库 permission 字段为 ENUM 类型，传入中文值失败 → 修改为中文枚举值 '仅自己可见''仅好友可见'‘所有人可见’ 并保持一致
```

#### 4.7

```
- 获取当前用户信息接口测试通过，返回除密码和时间的个人信息
- 修改个人信息接口测试通过，可修改昵称/头像/座右铭
- 修改密码功能测试通过（密码加密）
- 实现笔记详情查询与生成浏览历史记录
- 编辑笔记（PATCH）：支持灵活修改标题、内容、标签中的任意字段
- 全局异常处理器：统一捕获 RuntimeException，返回 Result 格式的错误响应
```

```
遇到的问题与解决
- 笔记更新时 update_time 自动填充未生效 → 在 Service 中手动note.setUpdateTime(LocalDateTime.now())
- 异常响应格式不一致 → 添加 GlobalExceptionHandler 全局异常处理器
```

#### 4.8

```
- 实现笔记逻辑删除功能（软删除）
- 修改笔记权限接口：支持更新为 仅自己可见 / 仅好友可见 / 所有人可见
- ThreadLocal重构：拦截器直接存储 userId（Long），Service 统一通过 ThreadLocalUtil.get() 获取，解决类型转换异常
```

```
遇到的问题与解决
- JWT 解析后 userId 类型为 Integer 导致强转异常 → 使用 ((Number) userIdObj).longValue() 
- 修改权限接口参数传递错误 → 采用 @PathVariable + @RequestBody 结合 DTO 方式
```



#### 4.9

```
- 实现查询最近浏览历史接口
  - 支持通过 `limit` 参数控制返回条数（默认10条）
  - 自动按浏览时间倒序排列
  - 批量查询笔记
  - 过滤已删除（逻辑/物理）和无权限的笔记
- 实现查看公开笔记接口（无需登录）
- 完善笔记详情权限校验：
  - 区分“仅自己可见”、“仅好友可见”、“所有人可见”
  - 集成 `FriendService.isFriend` 双向检查“仅好友可见”权限
- 实现搜索用户接口，返回'UserSearchVO'
```

```
遇到的问题与解决
- SQL 语法错误：`limit5` → 加上空格后 `limit 5` 解决
- 注册时邮箱/手机号同时必填，不灵活 → 修改 `RegisterRequest` 使用 `@AssertTrue` 至少提供一个，数据库允许 NULL。
- 搜索用户返回了敏感字段 → 创建 `UserSearchVO` 过滤
```

#### 4.10

```
- 实现发送好友申请接口
  - 校验目标用户是否存在且未注销
  - 禁止添加自己为好友
  - 禁止重复发送待处理的申请
  - 若已是好友，直接返回好友信息（`UserSearchVO`）
  - 申请成功后返回自定义成功消息：“好友申请发送成功，请等待对方同意。”
- 优化 `Result` 类，支持自定义成功消息
- 实现查看待处理好友申请列表接口
- 完善搜索用户接口：增加按 `username` 模糊搜索，支持手机号/邮箱/用户名查询
```

```
遇到的问题与解决
- 发送申请成功后如何返回不同提示 → 通过 `Result.success(String message, T data)` 重载实现
- 搜索用户时 `or` 与 `and` 优先级问题 → 使用 `wrapper.and(w -> w.eq(...).or().eq(...).or().like(...))` 解决
- 用户资料修改时 `update_time` 不更新
  - 原因：MyBatis-Plus 默认只更新非null字段，`updateTime` 有旧值导致被忽略
  - 解决方案：在实体类中使用 `@TableField(update = "now()")`，让数据库自动更新时间，替代自动填充处理器
```

#### 4.11

```
- 实现同意/拒绝好友申请两个接口
 - 同意：更新申请状态为1，同时插入双向好友记录（`friend` 表）
 - 拒绝：仅更新申请状态为2
 - 添加事务支持 `@Transactional`，保证数据一致性
- 实现查询好友列表接口
  - 支持分页（pageNum, pageSize）
  - 支持按分组筛选（group参数，可选）
  - 返回 FriendVO 包含好友ID、用户名、昵称、头像、座右铭、分组名
- 修复 `Result.success(String message, T data)` 方法中误传 null 的 bug
- 完成 AI 智能分析核心功能：
  - 使用 Spring WebClient 调用 DeepSeek API，为笔记生成摘要和要点
  - 构造 Prompt 强制要求输出纯 JSON 格式，并成功解析返回结果
  - 实现测试方法 `generateSummaryAndKeyPoints`，验证 API 调用链路正常
```

```
- 遇到的问题与解决
  - AI 返回内容被 Markdown 代码块包裹，导致 JSON 解析失败 → 在 Prompt 中明确要求“只输出纯JSON，不要用Markdown代码块”
```

